[project]
name = "dbdmanager-back"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TrappyMadz", email = "simon.charrier42@outlook.fr" }
]
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.31.0",
    "sqlalchemy>=2.0.51",
]

[project.scripts]
dbdmanager-back = "dbdmanager_back:main"
